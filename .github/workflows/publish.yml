name: Publish chiky to npm

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    if: github.repository == 'mrmx/Chiky'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          registry-url: 'https://registry.npmjs.org/'

      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter chiky check
      - run: pnpm --filter chiky build

      - name: Publish to npm
        working-directory: packages/chiky
        run: pnpm publish --access public --provenance --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        if: success()
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          gh release create "$GITHUB_REF_NAME" \
            --title "$GITHUB_REF_NAME" \
            --notes "## chiky@${VERSION}

          **npm:** [\`chiky@${VERSION}\`](https://www.npmjs.com/package/chiky/v/${VERSION})
          **Install:** \`npm install chiky@${VERSION}\`" \
            || echo "Release already exists"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
